rootProject.name = "shot-consumer-compose"
include ':app'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("../gradle/libs.versions.toml"))
            version("android-build-tools", "8.0.2")
        }
    }
}
